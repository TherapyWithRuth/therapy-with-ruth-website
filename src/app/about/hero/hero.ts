import { Component } from '@angular/core';

import { ScrollSnapSection } from '../../shared/scroll-snap-section';

@Component({
  selector: 'app-about-hero',
  templateUrl: './hero.html',
  styleUrl: './hero.scss',
})
export class AboutHero extends ScrollSnapSection {}
